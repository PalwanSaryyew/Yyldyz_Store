{
   "name": "main",
   "version": "1.0.0",
   "scripts": {
      "install": "cd .. && cd bot && npm install && npm link && cd .. && cd app && npm install && npm link bot && npx prisma generate && npm link && cd .. && cd bot && npm link app && cd .. && cd app",
      "build": "npx next build && npx next start",
      "start": "npx next start"
   },
   "keywords": [],
   "author": "",
   "license": "ISC",
   "description": ""
}
